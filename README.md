## Project Status

This repository is no longer maintained. The UI has moved to the new project: https://github.com/MoFox-Studio/MoFox-Core-Webui

## 项目状态

本仓库已停止维护，新的 UI 已迁移到：https://github.com/MoFox-Studio/MoFox-Core-Webui

## Running the code (legacy)

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
